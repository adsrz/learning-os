# Project

## Identity

- `project_name`:
- `workflow_mode`: multi-book synthesis
- `primary_goal`:
- `current_frontier`:

## Core Sources

- `source_1`:
- `source_2`:
- `source_3`:

## Source Scope

- `in_scope_sources`:
- `out_of_scope_sources`:
- `source_boundary_notes`:

## Operating Notes

- `durable_outputs_to_keep`: shared structure, source-specific tension, explicit module routing
- `validation_requirements`: the packet should not silently erase source differences or source leadership
- `write_back_targets`: `session-log.md`, `open-questions.md`, `distinctions.md`, `module-map.md`, `source-registry.json`
