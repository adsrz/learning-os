# Distinctions

- `source arrival` is not the same thing as `study packet readiness`
- `workflow routing` should happen before explanation depth expands
- a bounded intake pass should create durable state even when the source has only been lightly inspected
- a clean source boundary is part of the output, not just a caveat
