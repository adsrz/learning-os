# Distinctions

- `single-book deep reading` means one source owns the packet, not just one source happened to be read first
- `first framing` is not the same as `finished interpretation`
- when one source owns the packet, unresolved internal mechanism questions matter more than early comparison
- source ownership is part of the write-back, not only a silent reading assumption
